name: Pages

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Verify sources
        run: |
          set -euo pipefail
          test -f pluto.css
          test -f demo/index.html
          test -d styles
          for f in $(grep -oE '@import url\("[^"]+"\)' pluto.css | sed -E 's/@import url\("([^"]+)"\)/\1/'); do
            test -f "$f" || { echo "Missing imported CSS: $f"; exit 1; }
          done

      - name: Build minified bundle
        run: |
          set -euo pipefail
          npm ci
          npm run build
          test -s dist/pluto.min.css

      - name: Assemble site
        run: |
          set -euo pipefail
          rm -rf _site
          mkdir -p _site
          cp pluto.css _site/
          cp -r styles _site/
          cp -r dist _site/
          cp -r demo _site/
          cat > _site/index.html <<'HTML'
          <!doctype html>
          <html lang="en">
            <head>
              <meta charset="utf-8" />
              <title>Pluto Design System</title>
              <meta http-equiv="refresh" content="0; url=./demo/" />
              <link rel="canonical" href="./demo/" />
            </head>
            <body>
              <p>Redirecting to <a href="./demo/">the demo</a>…</p>
            </body>
          </html>
          HTML

      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
